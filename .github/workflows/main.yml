name: Build-Dylib
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: make
      - uses: actions/upload-artifact@v3
        with:
          name: Hang-Vip-OB53
          path: modff.dylib
